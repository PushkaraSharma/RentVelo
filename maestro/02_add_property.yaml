appId: com.indieroots.rentVelo
---
# 02_add_property.yaml
- launchApp:
    clearState: false # We want to stay logged in from the previous step

# Ensure we are on Dashboard
- assertVisible: "WELCOME BACK"

# Navigate to Properties
- tapOn: "Properties, tab, 2 of 3" # Assuming there's a bottom tab or a button called Properties

# Tap the Add Property button (+)
- tapOn: "Add Property" # Or you can use testID if it's an icon: tapOn: { id: "add-property-btn" }

# Wait for screen to load
- assertVisible: "Create New Property"

# Fill out the form
- tapOn: "PROPERTY NAME"
- inputText: "Maestro Test Villa"
- hideKeyboard

- tapOn: "ADDRESS"
- inputText: "123 Automation Lane"
- hideKeyboard

- swipe:
    direction: UP
    duration: 500

- tapOn: "TOTAL FLOORS"
- inputText: "2"
- hideKeyboard

# Select property type (assuming there's text 'House' or 'Flat')
- tapOn: "House"

# Tap create
- tapOn: "Create Property"

# Verify Success modal
- assertVisible: "Property Created!"

# Close modal
- tapOn: "Got it" # Or whatever the close button text is
